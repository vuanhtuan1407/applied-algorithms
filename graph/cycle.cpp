#include<bits/stdc++.h>

using namespace std;

// Undirected graph
template <typename T>
bool UG_Cycle(T s, int length);


// Directed graph
template <typename T>
bool DG_Cycle(T s, int length);

int main() {
    return 0;
}